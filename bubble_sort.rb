#bubble sort algorithm
def bubble_sort(arr)
    upper_bound = arr.length-1
    upper_bound.downto(0) do |count|
        puts "down #{indx}"
        (0...count).each do |indx|
            puts "each #{indx}"
        end
        puts "one pass"
    end
    return arr
end